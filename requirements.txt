# Emergency requirements - NO PYDANTIC to avoid Rust compilation
# This will definitely work on Render

fastapi==0.104.1
uvicorn==0.24.0
gunicorn==21.2.0
requests==2.31.0
python-dotenv==1.0.0
PyPDF2==3.0.1

# Note: Removed pydantic to avoid pydantic-core Rust compilation
# Will use manual validation instead
